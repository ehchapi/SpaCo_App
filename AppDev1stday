console.log()  = 화면에 값을 찍는 애

let 변수 = 바뀌어도 되는 변수

const 변수 = 한 번 정하면 죽을 때까지 처음 값

let a_list = []      목록을 정하고 그 안에 변수를 많이 넣을 수 있음. 0번째부터 세아림

a_list.push()    목록에 변수 추가

a_list.length    목록에 변수가 몇 개 있는지 확인

let a_dict = {"":""}    앞은 키, 뒤는 값

그 뒤에 추가할 때는 a_dict["키"] = 값 선언하면 추가된다.

let names = [{"name":"hyoin", "age":30},{"name":"minju", "age":25}]
console.log(names)

names.push({"name":"hyojun","age":22})
console.log(names)

names[1]["age"]   =  names의 2번째 사람의 나이

console.log (a % b)   =    a를 b로 나눈 나머지 값

구글에다가 검색해서 알아내는 거 중요!!!

join 합치다, split 분리하다

concat 기존 배열에서 새 배열로!

function 함수로 자기 자신의 함수를 만들 수 있따!

function minus(num1, num2){
return num1 - num2
}

let result = minus(100,10)
console.log(result)

let a = function(){

console.log("리터럴 함수 방식")

}

a()
